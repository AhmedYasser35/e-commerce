import { Spinner } from "@/components/ui/spinner";

export default function SpinnerSize() {
  return (
    <div className="flex items-center gap-6 w-full h-screen justify-center">
      <Spinner className="size-8" />
    </div>
  );
}
